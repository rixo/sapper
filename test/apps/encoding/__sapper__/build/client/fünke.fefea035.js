import { S as SvelteComponent, i as init, s as safe_not_equal, e as element, d as text, h as claim_element, j as children, k as claim_text, l as detach, m as insert, o as append, C as set_data, n as noop } from './chunk.7ccdb5fe.js';

/* test/apps/encoding/src/routes/fünke.html generated by Svelte v3.6.9 */

function create_fragment(ctx) {
	var h1, t;

	return {
		c() {
			h1 = element("h1");
			t = text(ctx.phrase);
		},

		l(nodes) {
			h1 = claim_element(nodes, "H1", {}, false);
			var h1_nodes = children(h1);

			t = claim_text(h1_nodes, ctx.phrase);
			h1_nodes.forEach(detach);
		},

		m(target, anchor) {
			insert(target, h1, anchor);
			append(h1, t);
		},

		p(changed, ctx) {
			if (changed.phrase) {
				set_data(t, ctx.phrase);
			}
		},

		i: noop,
		o: noop,

		d(detaching) {
			if (detaching) {
				detach(h1);
			}
		}
	};
}

function preload() {
	return this.fetch('fünke.json').then(r => r.json()).then(phrase => {
		return { phrase };
	});
}

function instance($$self, $$props, $$invalidate) {
	let { phrase } = $$props;

	$$self.$set = $$props => {
		if ('phrase' in $$props) $$invalidate('phrase', phrase = $$props.phrase);
	};

	return { phrase };
}

class F_nke extends SvelteComponent {
	constructor(options) {
		super();
		init(this, options, instance, create_fragment, safe_not_equal, ["phrase"]);
	}
}

export default F_nke;
export { preload };
