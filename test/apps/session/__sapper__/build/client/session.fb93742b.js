import { S as SvelteComponent, i as init, s as safe_not_equal, e as element, d as text, f as space, h as claim_element, j as children, k as claim_text, l as detach, B as listen, m as insert, o as append, C as set_data, n as noop, D as subscribe } from './chunk.23945acd.js';
import { b as stores$1 } from './chunk.76a4a249.js';

/* test/apps/session/src/routes/session.svelte generated by Svelte v3.6.9 */

function create_fragment(ctx) {
	var h1, t0_value = ctx.$session.title, t0, t1, button, t2, dispose;

	return {
		c() {
			h1 = element("h1");
			t0 = text(t0_value);
			t1 = space();
			button = element("button");
			t2 = text("click me");
			this.h();
		},

		l(nodes) {
			h1 = claim_element(nodes, "H1", {}, false);
			var h1_nodes = children(h1);

			t0 = claim_text(h1_nodes, t0_value);
			h1_nodes.forEach(detach);
			t1 = claim_text(nodes, "\n\n");

			button = claim_element(nodes, "BUTTON", {}, false);
			var button_nodes = children(button);

			t2 = claim_text(button_nodes, "click me");
			button_nodes.forEach(detach);
			this.h();
		},

		h() {
			dispose = listen(button, "click", ctx.click_handler);
		},

		m(target, anchor) {
			insert(target, h1, anchor);
			append(h1, t0);
			insert(target, t1, anchor);
			insert(target, button, anchor);
			append(button, t2);
		},

		p(changed, ctx) {
			if ((changed.$session) && t0_value !== (t0_value = ctx.$session.title)) {
				set_data(t0, t0_value);
			}
		},

		i: noop,
		o: noop,

		d(detaching) {
			if (detaching) {
				detach(h1);
				detach(t1);
				detach(button);
			}

			dispose();
		}
	};
}

function instance($$self, $$props, $$invalidate) {
	let $session;

	const { session } = stores$1(); subscribe($$self, session, $$value => { $session = $$value; $$invalidate('$session', $session); });

	function click_handler() {
		return session.set({ title: 'changed' });
	}

	return { session, $session, click_handler };
}

class Session extends SvelteComponent {
	constructor(options) {
		super();
		init(this, options, instance, create_fragment, safe_not_equal, []);
	}
}

export default Session;
