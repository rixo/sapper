import { S as SvelteComponent, i as init, s as safe_not_equal, e as element, d as text, h as claim_element, j as children, k as claim_text, l as detach, m as insert, o as append, C as set_data, n as noop, D as subscribe } from './chunk.7ccdb5fe.js';
import { b as stores$1 } from './chunk.8dc12362.js';

/* test/apps/custom-extension/src/routes/[slug].mdx generated by Svelte v3.6.9 */

function create_fragment(ctx) {
	var h1, t_value = ctx.$page.params.slug.toUpperCase(), t;

	return {
		c() {
			h1 = element("h1");
			t = text(t_value);
		},

		l(nodes) {
			h1 = claim_element(nodes, "H1", {}, false);
			var h1_nodes = children(h1);

			t = claim_text(h1_nodes, t_value);
			h1_nodes.forEach(detach);
		},

		m(target, anchor) {
			insert(target, h1, anchor);
			append(h1, t);
		},

		p(changed, ctx) {
			if ((changed.$page) && t_value !== (t_value = ctx.$page.params.slug.toUpperCase())) {
				set_data(t, t_value);
			}
		},

		i: noop,
		o: noop,

		d(detaching) {
			if (detaching) {
				detach(h1);
			}
		}
	};
}

function instance($$self, $$props, $$invalidate) {
	let $page;

	const { page } = stores$1(); subscribe($$self, page, $$value => { $page = $$value; $$invalidate('$page', $page); });

	return { page, $page };
}

class Slug extends SvelteComponent {
	constructor(options) {
		super();
		init(this, options, instance, create_fragment, safe_not_equal, []);
	}
}

export default Slug;
