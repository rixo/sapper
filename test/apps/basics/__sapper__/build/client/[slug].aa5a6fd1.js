import { S as SvelteComponent, i as init, s as safe_not_equal, e as element, d as text, f as space, h as claim_element, j as children, k as claim_text, l as detach, B as attr, m as insert, o as append, E as set_data, n as noop, F as subscribe } from './chunk.68ae2c07.js';
import { b as stores$1 } from './chunk.ef0dad12.js';

/* test/apps/basics/src/routes/[slug].svelte generated by Svelte v3.6.9 */

function create_fragment(ctx) {
	var h1, t0_value = ctx.$page.params.slug, t0, t1, a, t2;

	return {
		c() {
			h1 = element("h1");
			t0 = text(t0_value);
			t1 = space();
			a = element("a");
			t2 = text("regexp route");
			this.h();
		},

		l(nodes) {
			h1 = claim_element(nodes, "H1", {}, false);
			var h1_nodes = children(h1);

			t0 = claim_text(h1_nodes, t0_value);
			h1_nodes.forEach(detach);
			t1 = claim_text(nodes, "\n\n");

			a = claim_element(nodes, "A", { href: true }, false);
			var a_nodes = children(a);

			t2 = claim_text(a_nodes, "regexp route");
			a_nodes.forEach(detach);
			this.h();
		},

		h() {
			attr(a, "href", "234");
		},

		m(target, anchor) {
			insert(target, h1, anchor);
			append(h1, t0);
			insert(target, t1, anchor);
			insert(target, a, anchor);
			append(a, t2);
		},

		p(changed, ctx) {
			if ((changed.$page) && t0_value !== (t0_value = ctx.$page.params.slug)) {
				set_data(t0, t0_value);
			}
		},

		i: noop,
		o: noop,

		d(detaching) {
			if (detaching) {
				detach(h1);
				detach(t1);
				detach(a);
			}
		}
	};
}

function instance($$self, $$props, $$invalidate) {
	let $page;

	const { page } = stores$1(); subscribe($$self, page, $$value => { $page = $$value; $$invalidate('$page', $page); });

	return { page, $page };
}

class Slug extends SvelteComponent {
	constructor(options) {
		super();
		init(this, options, instance, create_fragment, safe_not_equal, []);
	}
}

export default Slug;
