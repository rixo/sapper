import { S as SvelteComponent, i as init, s as safe_not_equal, e as element, d as text, h as claim_element, j as children, k as claim_text, l as detach, m as insert, o as append, B as set_data, n as noop } from './chunk.1def16bf.js';

/* test/apps/export-queue/src/routes/b-[x].svelte generated by Svelte v3.6.9 */

function create_fragment(ctx) {
	var p, t0, t1;

	return {
		c() {
			p = element("p");
			t0 = text("b-");
			t1 = text(ctx.x);
		},

		l(nodes) {
			p = claim_element(nodes, "P", {}, false);
			var p_nodes = children(p);

			t0 = claim_text(p_nodes, "b-");
			t1 = claim_text(p_nodes, ctx.x);
			p_nodes.forEach(detach);
		},

		m(target, anchor) {
			insert(target, p, anchor);
			append(p, t0);
			append(p, t1);
		},

		p(changed, ctx) {
			if (changed.x) {
				set_data(t1, ctx.x);
			}
		},

		i: noop,
		o: noop,

		d(detaching) {
			if (detaching) {
				detach(p);
			}
		}
	};
}

function instance($$self, $$props, $$invalidate) {
	let { x } = $$props;

	$$self.$set = $$props => {
		if ('x' in $$props) $$invalidate('x', x = $$props.x);
	};

	return { x };
}

class B_x extends SvelteComponent {
	constructor(options) {
		super();
		init(this, options, instance, create_fragment, safe_not_equal, ["x"]);
	}
}

export default B_x;
